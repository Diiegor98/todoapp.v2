import './TodosEmpty.css'

const TodosEmpty = () => {
  return (
    <div className="empty-container">
        <p className="empty-text">Crea una nueva tarea para comenzar</p>
    </div>
  );
};

export default TodosEmpty;
